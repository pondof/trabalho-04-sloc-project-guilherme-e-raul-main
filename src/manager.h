#ifndef __MANAGER_
#define __MANAGER__

// when the user types -h or --help
// show the info table
void show_help(void);

// class responsible for reading single or multiple files
class manager : public fileInfo{
public:
    
};

manager::
// function responsible for showing file stats
manager::void show_file_stats(const std::string* message);
#endif __MANAGER__